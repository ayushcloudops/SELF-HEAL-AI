# =============================================================================
# Python dependencies for the AI Self-Healing Terraform Pipeline agent.
# Pinned to compatible ranges for reproducible CI runs on Python 3.11.
# =============================================================================

# HTTP client used for the Ollama API and the GitHub REST API.
requests>=2.31,<3.0

# YAML parsing for the knowledge-base files (optional but recommended).
PyYAML>=6.0,<7.0
